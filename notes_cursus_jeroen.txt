
In insert query "Addition" is given as column name, the insert statements have as name "Huisnummer2", which is confusing.


CREATE TABLE Customers(ClientNr int(8),Initials varchar(8), Firstname varchar(64),LastName varchar(64), Title varchar(64), Street varchar(64), Postalcode varchar(8), HouseNr int(8), Addition varchar(8), City varchar(64), CreationDate Date;