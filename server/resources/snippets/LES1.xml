<?xml version="1.0" encoding="UTF-8"?>
<module>
    <adapter name="" active="${HelloWorld.active}">
        <receiver name="HelloWorld" className="nl.nn.adapterframework.receivers.GenericReceiver">
            <listener name="HelloWorld" className="nl.nn.adapterframework.receivers.JavaListener"/>
        </receiver>
        <pipeline firstPipe=""/>
    </adapter>
    <scheduler>
        <job name="HelloWorld" adapterName="HelloWorld" receiverName="HelloWorld" interval="0" function="sendMessage" active="${HelloWorld.active}"/>
    </scheduler>
</module>